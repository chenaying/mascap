# 设置默认分支并删除旧分支 - 操作指南

## 方法一：使用 PowerShell 脚本（推荐）

1. **获取 GitHub Personal Access Token**：
   - 访问：https://github.com/settings/tokens
   - 点击 "Generate new token (classic)"
   - 勾选 `repo` 权限
   - 生成并复制 token

2. **运行脚本**：
   ```powershell
   powershell -ExecutionPolicy Bypass -File set_default_branch_simple.ps1
   ```
   - 按提示输入 GitHub token
   - 脚本会自动：
     - 将 `MeaCap_InvLM_origin` 设置为默认分支
     - 删除 `MeaCap_origin` 分支

## 方法二：使用 curl 命令（需要 token）

替换 `<YOUR_TOKEN>` 为您的 GitHub token：

```powershell
# 1. 设置默认分支
$token = "<YOUR_TOKEN>"
$owner = "chenaying"
$repo = "meacap_project"
$branch = "MeaCap_InvLM_origin"

curl.exe -X PATCH `
  -H "Authorization: token $token" `
  -H "Accept: application/vnd.github.v3+json" `
  -H "Content-Type: application/json" `
  -d "{\"default_branch\":\"$branch\"}" `
  "https://api.github.com/repos/$owner/$repo"

# 2. 删除旧分支
git push origin --delete MeaCap_origin
```

## 方法三：在 GitHub 网页上操作

### 步骤 1：设置默认分支

1. 访问：https://github.com/chenaying/meacap_project/settings/branches
2. 在 "Default branch" 部分，点击分支名称旁边的切换图标
3. 选择 `MeaCap_InvLM_origin` 分支
4. 点击 "Update" 按钮
5. 在确认对话框中点击 "I understand, update the default branch"

### 步骤 2：删除旧分支

1. 访问：https://github.com/chenaying/meacap_project/branches
2. 找到 `MeaCap_origin` 分支
3. 点击右侧的删除图标（垃圾桶图标）
4. 确认删除

## 验证操作

执行以下命令验证：

```powershell
# 查看远程分支
git ls-remote --heads origin

# 应该只看到 MeaCap_InvLM_origin 分支
```

## 注意事项

- 删除默认分支前，必须先更改默认分支
- GitHub 会阻止删除当前默认分支
- 确保 `MeaCap_InvLM_origin` 分支已成功推送


